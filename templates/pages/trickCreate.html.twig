{% extends 'layout.html.twig' %}

{% block pageTitle %}<h1>Ajoutez une nouvelle figure !</h1>{% endblock %}

{% block content %}
    <div class="container">
        <article>
            <p>
                {{form_start(form)}}
                    {{form_widget(form)}}

                    <input type="submit" value="Créer une nouvelle figure" />
                {{form_end(form)}}
            </p>
        </article>

        <p>
            <a href="{{path('app_index')}}">Retourner à la page d'accueil</a>
        </p>
    </div>
{% endblock %}

{% block pageFooter %}{% endblock %}
