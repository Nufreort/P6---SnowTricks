{% extends 'base.html.twig' %}

{% block pageTitle %}<h1 class="h3 mb-3 font-weight-bold">Mot de passe oublié</h1>{% endblock %}

{% block content %}
    <div class="container">
        <p>
          {{ form_start(resetForm) }}
              {{ form_row(resetForm.plainPassword) }}
              <button class="btn btn-primary">Reset password</button>
          {{ form_end(resetForm) }}
        </p>
    </div>
{% endblock %}
